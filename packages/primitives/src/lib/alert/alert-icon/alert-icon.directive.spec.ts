import { NgxPrimerAlertIconDirective } from './alert-icon.directive';

describe('NgxPrimerAlertIconDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxPrimerAlertIconDirective();
    expect(directive).toBeTruthy();
  });
});
