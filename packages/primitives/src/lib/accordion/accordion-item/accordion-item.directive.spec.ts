import { NgxPrimerAccordionItemDirective } from './accordion-item.directive';

describe('NgxPrimerAccordionItemDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxPrimerAccordionItemDirective();
    expect(directive).toBeTruthy();
  });
});
