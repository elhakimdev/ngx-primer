import { NgxPrimerAccordionTiggerDirective } from './accordion-tigger.directive';

describe('NgxPrimerAccordionTiggerDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxPrimerAccordionTiggerDirective();
    expect(directive).toBeTruthy();
  });
});
